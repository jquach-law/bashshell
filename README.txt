In 'smallsh' directory:

Makefile method:
    make, make run, make clean (compile, run, clean respectively)
    
Regular input
    1 Compile: gcc -std=gnu99 -Wall -g -o movies_by_year files_directories.c
    2 Run: movies_by_year
    3 Clean: rm -f movies_by_year